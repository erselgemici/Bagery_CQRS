<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bagery | Yönetim Paneli</title>

    <link href="~/assets/css/bootstrap.css" rel="stylesheet">
    <link href="~/assets/css/font-awesome-all.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* --- BAGERY MINT KONSEPT --- */
        :root {
            /* ANA RENK: MINT (Eskiden Rol Ata butonuydu) */
            --bagery-main: #9CD1CD;
            --bagery-main-dark: #82b8b4;
            /* ARA RENK: SOMON (Vurgular ve Ekleme Butonları) */
            --bagery-accent: #E69B88;
            --bagery-accent-dark: #d48a77;
            --bg-body: #f8fcfc; /* Çok uçuk nane/beyaz karışımı */
            --text-dark: #505050; /* Koyu Gri */
            --white: #ffffff;
        }

        body {
            background-color: var(--bg-body);
            font-family: 'Outfit', sans-serif;
            color: var(--text-dark);
        }

        .wrapper {
            display: flex;
            width: 100%;
            align-items: stretch;
        }

        /* --- SIDEBAR (MINT) --- */
        #sidebar {
            min-width: 260px;
            max-width: 260px;
            background: var(--bagery-main);
            color: var(--white);
            transition: all 0.3s;
            min-height: 100vh;
            box-shadow: 4px 0 15px rgba(156, 209, 205, 0.4);
            z-index: 100;
        }

            #sidebar .sidebar-header {
                padding: 25px 20px;
                background: rgba(0,0,0,0.05);
                text-align: center;
                border-bottom: 1px solid rgba(255,255,255,0.3);
            }

                #sidebar .sidebar-header h3 {
                    color: #fff;
                    font-weight: 700;
                    margin: 0;
                    font-size: 1.8rem;
                    letter-spacing: 1px;
                    text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
                }

            #sidebar ul.components {
                padding: 15px 0;
            }

        /* Menü Başlıkları */
        .menu-label {
            color: rgba(255,255,255,0.7);
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            padding: 15px 25px 5px 25px;
            font-weight: 600;
        }

        #sidebar ul li a {
            padding: 12px 25px;
            font-size: 0.95em;
            display: flex;
            align-items: center;
            color: rgba(255,255,255,0.95);
            text-decoration: none;
            transition: 0.3s;
            font-weight: 500;
        }

            #sidebar ul li a i {
                margin-right: 12px;
                width: 25px;
                text-align: center;
                font-size: 1.1em;
            }

            /* Hover Durumu */
            #sidebar ul li a:hover, #sidebar ul li.active > a {
                background: rgba(255,255,255,0.2);
                color: #fff;
                padding-left: 30px; /* Sağa kayma efekti */
            }

        /* --- İÇERİK ALANI --- */
        #content {
            width: 100%;
            padding: 40px;
            background: var(--bg-body);
        }

        /* Üst Bar */
        .top-navbar {
            background: var(--white);
            padding: 15px 30px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(156, 209, 205, 0.1);
            margin-bottom: 30px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            border-top: 3px solid var(--bagery-main);
        }

        /* Kartlar */
        .card-custom {
            border: none;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.03);
            background: var(--white);
            padding: 35px;
            margin-bottom: 30px;
        }

        /* Başlıklar (Mint Rengi) */
        .admin-title {
            color: var(--bagery-main-dark);
            font-weight: 700;
            display: flex;
            align-items: center;
            font-size: 1.6rem;
            margin-bottom: 25px;
            border-bottom: 2px dashed rgba(156, 209, 205, 0.5);
            padding-bottom: 15px;
        }

            .admin-title i {
                margin-right: 12px;
                color: var(--bagery-accent);
            }

        /* --- TABLO DÜZENLEMELERİ --- */
        .table thead th {
            background-color: var(--bagery-main); /* Tablo Başlığı MINT */
            color: var(--white);
            border: none;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.85rem;
            padding: 15px;
            border-radius: 10px 10px 0 0;
        }

            .table thead th:first-child {
                border-radius: 10px 0 0 0;
            }

            .table thead th:last-child {
                border-radius: 0 10px 0 0;
            }

            .table thead th:not(:first-child):not(:last-child) {
                border-radius: 0;
            }

        .table tbody td {
            padding: 15px;
            vertical-align: middle;
            border-bottom: 1px solid #f0f0f0;
            color: #666;
            font-weight: 500;
        }

        /* --- BUTONLAR --- */
        /* Ana Ekleme Butonu (SOMON - Dikkat çeksin diye) */
        .btn-bagery-primary {
            background: var(--bagery-accent);
            color: #fff;
            border-radius: 50px;
            padding: 10px 25px;
            font-weight: 600;
            border: none;
            transition: 0.3s;
            box-shadow: 0 4px 10px rgba(230, 155, 136, 0.4);
        }

            .btn-bagery-primary:hover {
                background: var(--bagery-accent-dark);
                color: #fff;
                transform: translateY(-2px);
            }

        /* İkincil Butonlar (Mint) */
        .btn-bagery-mint {
            background: var(--bagery-main);
            color: #fff;
            border-radius: 8px;
            padding: 8px 15px;
            font-weight: 500;
            border: none;
            transition: 0.3s;
        }

            .btn-bagery-mint:hover {
                background: var(--bagery-main-dark);
                color: #fff;
            }

        /* Silme Butonu */
        .btn-action-delete {
            color: #ff6b6b;
            background: #fff0f0;
            border: 1px solid #ffcccc;
            border-radius: 8px;
            padding: 8px 12px;
            transition: 0.3s;
        }

            .btn-action-delete:hover {
                background: #ff6b6b;
                color: #fff;
            }

    </style>
</head>
<body>

    <div class="wrapper">
        <nav id="sidebar" class="sidebar">
            <div class="sidebar-header">
                <h3><i class="fas fa-cookie-bite" style="color: #fff;"></i> Bagery</h3>
                <p style="font-size: 0.9em; opacity: 0.8;">Admin Paneli</p>
            </div>

            <ul class="list-unstyled components">

                <li class="sidebar-header-sm">Mağaza Yönetimi</li>

                <li>
                    <a href="/Admin/Category/Index"><i class="fas fa-tags"></i> Kategoriler</a>
                </li>
                <li>
                    <a href="/Admin/Product/Index"><i class="fas fa-birthday-cake"></i> Ürünler</a>
                </li>
                <li>
                    <a href="#"><i class="fas fa-receipt"></i> Siparişler</a>
                </li>
                <li>
                    <a href="/Admin/Promotion/Index"><i class="fas fa-percent"></i> Kampanyalar</a>
                </li>

                <li class="sidebar-header-sm">Sayfa İçeriği</li>

                <li>
                    <a href="/Admin/Slider/Index"><i class="fas fa-images"></i> Slider (Manşet)</a>
                </li>
                <li>
                    <a href="/Admin/About/Index"><i class="fas fa-info-circle"></i> Hakkımızda</a>
                </li>
                <li>
                    <a href="/Admin/Feature/Index"><i class="fas fa-star"></i> Öne Çıkanlar</a>
                </li>
                <li>
                    <a href="/Admin/Service/Index"><i class="fas fa-concierge-bell"></i> Hizmetler</a>
                </li>
                <li>
                    <a href="/Admin/PhotoGallery/Index"><i class="fas fa-camera"></i> Foto Galeri</a>
                </li>
                <li>
                    <a href="/Admin/Testimonial/Index"><i class="fas fa-comments"></i> Referanslar</a>
                </li>
                <li>
                    <a href="/Admin/News/Index"><i class="fas fa-newspaper"></i> Haberler</a>
                </li>

                <li class="sidebar-header-sm">İletişim & Destek</li>

                <li>
                    <a href="/Admin/ContactInfo/Index"><i class="fas fa-map-marker-alt"></i> Adres Bilgileri</a>
                </li>
                <li>
                    <a href="/Admin/Contact/Index"><i class="fas fa-envelope"></i> Gelen Kutusu</a>
                </li>

                <li class="sidebar-header-sm">Sistem Ayarları</li>

                <li>
                    <a href="/Admin/Role/UserList"><i class="fas fa-users"></i> Kullanıcılar</a>
                </li>
                <li>
                    <a href="/Admin/Role/Index"><i class="fas fa-user-shield"></i> Roller</a>
                </li>
                <li>
                    <a href="/Admin/Log/Index"><i class="fas fa-history"></i> Sistem Logları</a>
                </li>

                <li class="mt-4 pt-3" style="border-top: 1px solid rgba(255,255,255,0.15);">
                    <a href="/Home/Index" target="_blank"><i class="fas fa-external-link-alt"></i> Siteye Git</a>
                </li>
                <li>
                    <a href="#"><i class="fas fa-sign-out-alt"></i> Çıkış Yap</a>
                </li>
            </ul>
        </nav>

        <div id="content">
            <div class="top-navbar">
                <div style="text-align: right; margin-right: 15px;">
                    <span style="display: block; font-weight: 700; color: var(--bagery-main-dark);">Admin Şef</span>
                    <span style="display: block; font-size: 12px; color: #999;">Sistem Yöneticisi</span>
                </div>
                <div style="width: 45px; height: 45px; background: var(--bagery-accent); color: #fff; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 20px; box-shadow: 0 3px 10px rgba(230, 155, 136, 0.4);">
                    <i class="fas fa-user"></i>
                </div>
            </div>

            @RenderBody()
        </div>
    </div>

    <script src="~/assets/js/jquery.js"></script>
    <script src="~/assets/js/bootstrap.min.js"></script>

    <script>
        $(document).on('click', '.btn-delete-confirm, .btn-delete', function (e) {
            e.preventDefault();
            var deleteUrl = $(this).attr('href');

            Swal.fire({
                title: 'Emin misiniz?',
                text: "Bu kayıt kalıcı olarak silinecektir!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#E69B88', /* Silme Onayı için Somon */
                cancelButtonColor: '#9CD1CD', /* Vazgeç için Mint */
                confirmButtonText: 'Evet, Sil!',
                cancelButtonText: 'Vazgeç',
                background: '#fff',
                color: '#505050'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = deleteUrl;
                }
            });
        });
    </script>

</body>
</html>
